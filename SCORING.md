# 收录评分规则

精选清单使用 100 分制。评分用于说明取舍，不代表安全认证。

| 维度 | 分值 | 判断内容 |
|---|---:|---|
| 工作价值 | 30 | 是否解决高频、耗时或容易出错的真实工程任务 |
| Pi/OpenCode 适配 | 20 | 标准格式、目录、工具名和依赖是否适配 |
| 维护状态 | 15 | 最近提交、Release、Issue 响应和维护连续性 |
| 来源与许可证 | 15 | 维护者可追溯、许可证清楚、来源可信 |
| 安全与可控性 | 15 | 权限、网络、脚本、凭据和副作用是否容易审查 |
| 生态采用度 | 5 | 安装量、使用案例和社区反馈；热度权重刻意保持较低 |

## 等级

| 总分 | 等级 | 建议 |
|---:|---|---|
| 85–100 | 推荐 | 可进入团队试点，仍需按固定 commit 审查 |
| 70–84 | 候选 | 在隔离仓库验证后再决定 |
| 50–69 | 观察 | 记录变化，暂不进入默认组合 |
| 0–49 | 不收录 | 风险、维护或实际价值证据不足 |

## 独立状态字段

分数之外还必须记录：

- `source_path`：是否已在原仓库核对具体 `SKILL.md`；
- `install_syntax`：安装命令是否符合当前官方 CLI 文档；
- `runtime_pi`：是否在 Pi 中实际加载和运行；
- `runtime_opencode`：是否在 OpenCode 中实际加载和运行；
- `runtime_evidence`：实机状态为 `passed` 时对应的测试记录；
- `risk`：低、中、高风险；
- `last_verified`：最近核验日期；
- `commit`：审查时观察到的第三方 commit；
- `pinning_status`：每个条目独立记录固定版本是已验证、待验证还是本仓库 `main`。

“路径已核验”或“安装语法已核验”不能写成“实机测试通过”。
