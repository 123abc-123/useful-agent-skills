# Regression Cases

这里保存 Skill 和 Prompt 的轻量回归案例。案例不调用模型，也不声称某个 Prompt 已在所有模型上稳定通过；它定义了人工或评测工具运行时应检查的行为边界。

`cases/prompts.json` 中每个 Prompt 至少有一个必须满足的断言和一个禁止行为。修改模板后，应在 Pi、OpenCode 或团队批准的模型上运行对应案例，并把模型、版本、日期和结果记录到独立报告中。

当前 CI 检查案例是否覆盖全部 Prompt、目标名称是否有效，以及正反断言是否齐全。
