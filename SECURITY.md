# 安全策略

本仓库收录第三方 Agent Skill，也维护少量无脚本 Skill。目录中的“推荐”表示值得评估，不代表第三方代码已经通过安全认证。

## 报告安全问题

请通过 GitHub 的私密漏洞报告功能提交会影响本仓库用户的问题。不要在公开 Issue 中粘贴密钥、内部路径、客户数据或可直接利用的凭据。

报告中请包含：

- 受影响的 Skill、文件与固定 commit；
- 触发方式和可能影响；
- 涉及的命令、网络地址、凭据或文件范围；
- 可安全公开的最小复现步骤；
- 建议的缓解措施。

## 收录安全边界

- 自动更新不会执行候选仓库中的脚本；
- `source_path=verified` 只表示具体文件已经找到并阅读；
- `install_syntax=verified` 只表示安装命令符合当前文档；
- `runtime_pi=passed` 或 `runtime_opencode=passed` 必须链接到实机测试记录；
- 固定 commit 变化后需要重新检查脚本、依赖、网络、凭据和高影响操作；
- 发现重大风险时，条目会降级、移入观察清单或删除，并记录到 `CHANGELOG.md`。

## 使用者检查清单

安装第三方 Skill 前，请确认它不会在未授权情况下读取用户目录、SSH key、浏览器 Cookie、云凭据或环境变量，不会运行 `curl | sh`，也不会自动发布、部署、合并、删除或发起付费任务。
